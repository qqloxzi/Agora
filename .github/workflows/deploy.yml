name: Hostinger_FTP_Deployment

on:
  push:
    branches: [ main ]

jobs:
  web-deploy:
    name: 🚀 Hostinger'a Gönder
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Kodu Al
      uses: actions/checkout@v4

    - name: 🟢 Node.js Kur
      uses: actions/setup-node@v4
      with:
        node-version: 20

    - name: 🔨 Bağımlılıkları Yükle
      run: npm install 
    - name: 🏗️ Projeyi Derle
      run: npm run build

    - name: 📂 Hostinger FTP Transfer
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        local-dir: ./dist/
        server-dir: ./public_html/ # Bu dizin Hostinger'da dosyaların olduğu yerdir