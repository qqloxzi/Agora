name: Deploy to Hostinger

on:
  push:
    branches: [ main ] # Ana dalınızın adı 'master' ise burayı değiştirin

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v4

    - name: 🟢 Use Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18' # Projenizin Node versiyonu

    - name: 🔨 Install dependencies
      run: npm install

    - name: 🏗️ Build Project
      run: npm run build

    - name: 📂 Sync files to Hostinger
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        local-dir: ./dist/ # Astro'nun çıktı klasörü
        server-dir: ./public_html/ # Hostinger'daki ana klasörünüz (gerekirse alt klasör yazın)
