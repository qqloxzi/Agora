---
import Layout from "@layouts/Layout.astro";
import { problemSet } from '../data/problems';
import GameManager from '../components/GameManager.jsx';
---

<Layout title="Go Ağacı">
  {/* SADECE BU KADAR! */}
  {/* client:only="react" komutu bu bileşeni tarayıcıda çalıştırır, sunucuyu yormaz */}
  
  <GameManager 
    client:only="react" 
    allProblems={problemSet} 
  />

</Layout>