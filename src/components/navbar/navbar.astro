---
import { Astronav, MenuItems, MenuIcon } from "astro-navbar";

const menuitems = [
  { title: "Ligler", path: "/ligler" },
  { title: "Planlar", path: "/pricing" },
  { title: "Go Ağacı", path: "/goagaci" },
  { title: "Kütüphane", path: "/blog" },
  { title: "Hakkında", path: "/about" }
];
---

<header class="fixed top-0 left-0 w-full z-50 bg-white dark:bg-gray-900 border-b border-gray-100 dark:border-gray-800 shadow-sm h-20 transition-all">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 h-full">
    
    <Astronav>
      <div class="flex items-center justify-between w-full h-full">
        
        {/* --- 1. SOL TARAFI (Mobil Menü Butonu + Logo) --- */}
        <div class="flex-1 flex items-center justify-start gap-4">
            
            {/* Mobil Menü Butonu (Sadece Mobilde Görünür) */}
            <div class="block lg:hidden text-gray-800 dark:text-gray-200">
              <MenuIcon class="w-8 h-8 cursor-pointer" />
            </div>

            {/* Logo (Hem Mobil Hem Desktop) */}
            <a href="/" class="flex items-center gap-2">
                <img src="/for-light-logo.svg" alt="Agora Logo" class="h-8 lg:h-10 w-auto object-contain block dark:hidden" />
                <img src="/for-dark-logo.svg" alt="Agora Dark Logo" class="h-8 lg:h-10 w-auto object-contain hidden dark:block" />
            </a>
        </div>

        {/* --- 2. ORTA KISIM (Sadece Masaüstü Menü Linkleri) --- */}
        {/* DİKKAT: Burayı MenuItems içinden çıkardık, böylece mobilde açılmayacak */}
        <div class="hidden lg:flex flex-none justify-center">
            <ul class="flex items-center gap-8"> 
            {menuitems.map((item) => {
                const isSpecial = item.title === "Go Ağacı";
                return (
                <li class="relative flex items-center">
                    <a href={item.path} class={`transition-all duration-300 whitespace-nowrap ${isSpecial ? "text-lg font-extrabold text-indigo-600 dark:text-indigo-400 scale-110 hover:scale-125 drop-shadow-sm" : "text-gray-600 dark:text-gray-300 hover:text-blue-600 dark:hover:text-blue-400 font-medium hover:scale-105"}`}>
                        {item.title}
                    </a>
                </li>
                )
            })}
            </ul>
        </div>

        {/* --- 3. SAĞ TARAF (Auth + Tema Butonu) --- */}
        <div class="flex-1 flex items-center justify-end gap-3 lg:gap-4">
          
            {/* Masaüstü Auth Butonları (Mobilde Gizli) */}
            <div class="hidden lg:flex items-center gap-4">
                <div id="nav-auth-container" class="flex items-center">
                    <a id="nav-login-btn" href="/signup" class="whitespace-nowrap px-6 py-2.5 text-sm font-medium text-white bg-slate-800 hover:bg-slate-700 rounded-full transition-colors shadow-sm hover:shadow-md">
                        Giriş Yap
                    </a>
                    <a id="nav-profile-btn" href="/profile" class="hidden flex items-center gap-2 p-1 pr-3 rounded-full hover:bg-gray-100 dark:hover:bg-gray-800 transition-all border border-transparent hover:border-gray-200">
                        <div class="w-8 h-8 rounded-full bg-blue-100 dark:bg-blue-900 flex items-center justify-center text-blue-600 dark:text-blue-300">
                            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="w-5 h-5">
                                <path fill-rule="evenodd" d="M7.5 6a4.5 4.5 0 119 0 4.5 4.5 0 01-9 0zM3.751 20.105a8.25 8.25 0 0116.498 0 .75.75 0 01-.437.695A18.683 18.683 0 0112 22.5c-2.786 0-5.433-.608-7.812-1.7a.75.75 0 01-.437-.695z" clip-rule="evenodd" />
                            </svg>
                        </div>
                        <span id="nav-user-name" class="text-sm font-medium text-gray-700 dark:text-gray-200">Profilim</span>
                    </a>
                </div>
                {/* Ayırıcı Çizgi */}
                <div class="h-6 w-px bg-gray-200 dark:bg-gray-700"></div>
            </div>

            {/* Tema Butonu (HER ZAMAN GÖRÜNÜR) */}
            <button id="theme-toggle" class="p-2 rounded-full hover:bg-gray-100 dark:hover:bg-gray-800 text-gray-600 dark:text-gray-300 transition-all focus:outline-none z-50">
                <svg id="theme-toggle-light-icon" class="hidden w-6 h-6" fill="currentColor" viewBox="0 0 20 20"><path d="M10 2a1 1 0 011 1v1a1 1 0 11-2 0V3a1 1 0 011-1zm4 8a4 4 0 11-8 0 4 4 0 018 0zm-.464 4.95l.707.707a1 1 0 001.414-1.414l-.707-.707a1 1 0 00-1.414 1.414zm2.12-10.607a1 1 0 010 1.414l-.706.707a1 1 0 11-1.414-1.414l.707-.707a1 1 0 011.414 0zM17 11a1 1 0 100-2h-1a1 1 0 100 2h1zm-7 4a1 1 0 011 1v1a1 1 0 11-2 0v-1a1 1 0 011-1zM5.05 6.464A1 1 0 106.465 5.05l-.708-.707a1 1 0 00-1.414 1.414l.707.707zm1.414 8.486l-.707.707a1 1 0 01-1.414-1.414l.707-.707a1 1 0 011.414 1.414zM4 11a1 1 0 100-2H3a1 1 0 000 2h1z"></path></svg>
                <svg id="theme-toggle-dark-icon" class="hidden w-6 h-6" fill="currentColor" viewBox="0 0 20 20"><path d="M17.293 13.293A8 8 0 016.707 2.707a8.001 8.001 0 1010.586 10.586z"></path></svg>
            </button>

        </div>
      </div>

      {/* --- MOBİL MENÜ OVERLAY (TAM EKRAN) --- */}
      {/* Sadece burası MenuItems içinde */}
      <MenuItems class="hidden lg:hidden">
        <div class="fixed top-20 left-0 w-full h-[calc(100vh-5rem)] bg-white dark:bg-gray-900 overflow-y-auto border-t border-gray-100 dark:border-gray-800 z-40">
            <ul class="flex flex-col p-6 gap-6 text-center mt-10">
            {menuitems.map((item) => (
                <li>
                    <a href={item.path} class="block text-2xl font-semibold text-gray-700 dark:text-gray-200 hover:text-indigo-600 dark:hover:text-indigo-400 transition-colors">
                        {item.title}
                    </a>
                </li>
            ))}
            
            <li class="mt-8 pt-8 border-t border-gray-100 dark:border-gray-700 w-full max-w-xs mx-auto">
                <a id="mobile-nav-login-btn" href="/signup" class="block w-full py-4 text-center text-lg text-white bg-slate-800 rounded-full font-bold shadow-lg">
                    Giriş Yap
                </a>

                <a id="mobile-nav-profile-btn" href="/profile" class="hidden flex items-center justify-center gap-3 py-3 w-full text-center text-indigo-600 dark:text-indigo-400 bg-indigo-50 dark:bg-indigo-900/30 rounded-full font-bold border border-indigo-100 dark:border-indigo-800">
                    <div class="w-8 h-8 rounded-full bg-indigo-200 flex items-center justify-center text-indigo-700 text-sm">
                        👤
                    </div>
                    <span id="mobile-user-name" class="text-lg">Profilim</span>
                </a>
            </li>
            </ul>
        </div>
      </MenuItems>

    </Astronav>
  </div>
</header>
<div class="h-20"></div>

<script>
  import { supabase } from 'lib/supabase';

  async function checkNavbarAuth() {
    try {
      const loginBtn = document.getElementById('nav-login-btn');
      const profileBtn = document.getElementById('nav-profile-btn');
      const userNameSpan = document.getElementById('nav-user-name');
      const mobileLoginBtn = document.getElementById('mobile-nav-login-btn');
      const mobileProfileBtn = document.getElementById('mobile-nav-profile-btn');
      const mobileUserNameSpan = document.getElementById('mobile-user-name');

      const { data: { user } } = await supabase.auth.getUser();

      if (user) {
        if(loginBtn) loginBtn.classList.add('hidden');
        if(profileBtn) {
            profileBtn.classList.remove('hidden');
            profileBtn.style.display = 'flex';
        }
        if(mobileLoginBtn) mobileLoginBtn.classList.add('hidden');
        if(mobileProfileBtn) {
            mobileProfileBtn.classList.remove('hidden');
            mobileProfileBtn.classList.add('flex');
        }

        let displayName = "Profilim";
        const { data: profile } = await supabase
            .from('agorasusers')
            .select('username')
            .eq('id', user.id)
            .single();
        
        displayName = profile?.username || user.email?.split('@')[0] || "Profilim";
        if (displayName.length > 15) displayName = displayName.substring(0, 12) + '...';

        if (userNameSpan) userNameSpan.innerText = displayName;
        if (mobileUserNameSpan) mobileUserNameSpan.innerText = displayName;

      } else {
        if(loginBtn) loginBtn.classList.remove('hidden');
        if(profileBtn) profileBtn.classList.add('hidden');
        if(mobileLoginBtn) mobileLoginBtn.classList.remove('hidden');
        if(mobileProfileBtn) mobileProfileBtn.classList.add('hidden');
      }
    } catch (err) {
      console.error("Navbar Script Hatası:", err);
    }
  }

  checkNavbarAuth();

  const themeToggleDarkIcon = document.getElementById('theme-toggle-dark-icon');
  const themeToggleLightIcon = document.getElementById('theme-toggle-light-icon');
  const themeToggleBtn = document.getElementById('theme-toggle');

  const currentTheme = (() => {
      if (typeof localStorage !== 'undefined' && localStorage.getItem('color-theme')) {
          return localStorage.getItem('color-theme');
      }
      if (window.matchMedia('(prefers-color-scheme: dark)').matches) {
          return 'dark';
      }
      return 'light';
  })();

  if (currentTheme === 'dark') {
      document.documentElement.classList.add('dark');
      document.documentElement.classList.remove('light'); 
      themeToggleLightIcon?.classList.remove('hidden');
  } else {
      document.documentElement.classList.remove('dark');  
      document.documentElement.classList.add('light');    
      themeToggleDarkIcon?.classList.remove('hidden');
  }

  themeToggleBtn?.addEventListener('click', () => {
      themeToggleDarkIcon?.classList.toggle('hidden');
      themeToggleLightIcon?.classList.toggle('hidden');

      if (document.documentElement.classList.contains('dark')) {
          document.documentElement.classList.remove('dark');
          document.documentElement.classList.add('light'); 
          localStorage.setItem('color-theme', 'light');
      } 
      else {
          document.documentElement.classList.remove('light'); 
          document.documentElement.classList.add('dark');
          localStorage.setItem('color-theme', 'dark');
      }
  });
</script>